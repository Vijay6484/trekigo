{
  "name": "trekigo",
  "private": true,
  "scripts": {
    "website": "npm run dev --prefix website",
    "admin": "npm run dev --prefix admin",
    "backend": "npm start --prefix backend",
    "backend:dev": "npm run nodemon --prefix backend",
    "db:up": "docker compose up -d",
    "db:seed": "docker compose exec -T mysql mysql -utrekigo -ptrekigo trekigo < backend/schema.sql"
  }
}
